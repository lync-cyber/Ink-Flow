# quotes

未启用：不在 target_platforms=[wechat] + content_column=tech 的任一 atomSelection 中。

constraints.columnPlatforms.tech.wechat.atomSelection = [claims, evidence-data, evidence-code, cases, pitfalls, comparison, actions]

quotes 不在此列表内，本次不产出原子。
